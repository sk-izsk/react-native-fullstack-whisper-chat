# backend

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run dev
```

To build and run with Node:

```bash
bun run build
node dist/index.js
```

This project was created using `bun init` in bun v1.3.7. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
